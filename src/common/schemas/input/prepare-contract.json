{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "prepareContractParameters",
  "type": "object",
  "properties": {
    "address": {
      "$ref": "address",
      "description": "The address of the account that is creating the transaction."
    },
    "contract": {
      "$ref": "contract",
      "description": "The specification of the contract to prepare."
    },
    "instructions": {"$ref": "instructions"}
  },
  "additionalProperties": false,
  "required": ["address", "contract"]
}
